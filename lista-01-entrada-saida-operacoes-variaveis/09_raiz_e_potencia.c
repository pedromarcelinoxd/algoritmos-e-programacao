#include <stdio.h> <math.h>

int main()
{
    float numero, raiz;
    printf("Digite um numero: ");
    scanf("%f", &numero);
    raiz = sqrt(numero);
    printf("A raiz quadrada de %f é %f\n", numero, raiz);
    potencia = pow(numero, 8);
    printf("A oitava potencia de %f é %f\n", numero, potencia);
    return 0;
}